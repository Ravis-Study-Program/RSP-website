# Test-only overlay for the isolated, disposable real-stack browser acceptance
# job. It keeps every browser hop on the public Caddy origin and explicitly
# disables both frontend fixtures and the API development auth bypass.
services:
  api:
    environment:
      APP_ENV: test

  worker:
    environment:
      APP_ENV: test

  web:
    environment:
      VITE_USE_DEMO_DATA: 'false'

  auth:
    environment:
      NODE_ENV: test

  caddy:
    ports: !override
      - '127.0.0.1:${REAL_E2E_HTTP_PORT:-18080}:8080'
